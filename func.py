def func(X):
	X = 0
	RESULT = NUM
	while X > NUM:
		NUM = 1 + NUM
		for EXECUTE in range(5):
			RESULT = RESULT + NUM
			
		RESULT = RESULT * NUM
		if X >= NUM:
			X = NUM
			
		else:
			if X > NUM:
				X = 500
				
			else:
				if X > 500:
					X = 500
					
				
			
		Z = RESULT
		
	
	return Z
